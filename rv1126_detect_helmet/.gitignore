build/
runs/
.venv/
__pycache__/
*.pyc
*.o
rv1126_detect_helmet_app
# Model artifacts and the helmet dataset are intentionally versioned so this
# directory remains reproducible when copied or cloned on its own.
